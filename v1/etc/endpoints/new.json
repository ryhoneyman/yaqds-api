{
   "/new/lookup/{lookup}": {
      "GET": {
         "name": "Perform NEW Lookup",
         "description": "",
         "controller": "NEWController",
         "function": "lookup",
         "category": "New Service",
         "filter": { "lookup": { "required": true, "constraint": "string", "description": "The NEW information to lookup." } },
         "param": {},
         "response": { "lookup": { "type": "string", "description": "The NEW that was requested" }, "results": { "type": "array", "description": "The NEW details" } }
      }
   },
   "/new/lookup/{lookup}/a": {
      "GET": {
         "name": "Perform NEW Forward Lookup",
         "description": "",
         "controller": "NEWController",
         "function": "forwardLookup",
         "category": "New Service",
         "filter": { "lookup": { "required": true, "constraint": "string", "description": "The NEW information to lookup." } },
         "param": {},
         "response": { "lookup": { "type": "string", "description": "The NEW that was requested" }, "results": { "type": "array", "description": "The NEW details" } }
      }
   },
   "/new/lookup/{lookup}/ptr": {
      "GET": {
         "name": "Perform NEW Reverse Lookup",
         "description": "",
         "controller": "NEWController",
         "function": "reverseLookup",
         "category": "New Service",
         "filter": { "lookup": { "required": true, "constraint": "string", "description": "The NEW information to lookup." } },
         "param": {},
         "response": { "lookup": { "type": "string", "description": "The NEW that was requested" }, "results": { "type": "array", "description": "The NEW details" } }
      }
   }
}
